// 逆順に並び替える
const array = [10, 0, 30];
array.reverse();
console.log(array);

// 文字列の昇順と降順について
const fruits = ["banana","lemon","apple"];
fruits.sort();
console.log(fruits);
fruits.reverse();
console.log(fruits);

// 文字列は一度昇順にしてからsort から reverseで降順に並び変えるのが一般的