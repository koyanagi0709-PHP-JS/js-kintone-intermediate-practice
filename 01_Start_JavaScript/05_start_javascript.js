const array = [10,20,30];

// 追加 unshift / push
array.unshift(1); // 先頭に要素を追加
console.log(array);

array.push(40); // 末尾に追加する
console.log(array);

// 削除 shift / pop
array.shift(); // 先頭の要素を削除する
console.log(array);

array.pop(); // 末尾の要素を削除する
console.log(array);